[build]
  publish = "frontend"
  command = "echo 'No build needed'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# 환경 변수는 Netlify 대시보드에서 설정하세요
# API_BASE_URL=https://your-app.up.railway.app

